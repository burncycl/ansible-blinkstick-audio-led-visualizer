---

- name: Install Blinkstick Vizsualizer 
  hosts: transmit_node, receive_nodes
  remote_user: pi 
  become: yes
  become_method: sudo
  vars_files:
   - secrets.yml 
  roles:
    - common 
    - blinkstickviz 
    - updates 
    - reboot 
